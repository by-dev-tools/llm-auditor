# Ground truth for the assumption-auditor eval harness.
#
# Each entry pairs a captured session fixture with the elements an auditor
# pass against it must contain. Matching is substring/regex on rendered
# output; the goal is regression detection, not benchmark-grade scoring.
#
# The first three entries below are the canonical examples from the build
# handoff (audit-examples.md). Their fixtures are not yet captured; they
# will be SKIPped until someone drops the .jsonl in fixtures/ and writes a
# matching .expected.txt.
#
# The synthetic_* entries are minimal hand-written fixtures included with
# the initial scaffold so the harness has something to actually exercise.
# Replace with real captures when available.

- case_id: trio_navigation_stack_cycle_3
  fixture: fixtures/trio_navigation_stack.jsonl
  mode: completion
  required:
    - category: "Unverified diagnosis"
    - claim_contains: "safeAreaPadding"
    - gap_mentions: ["simulator", "screenshot", "visual"]
    - falsifier_present: true

- case_id: portfolio_blank_screen
  fixture: fixtures/portfolio_blank_screen.jsonl
  mode: completion
  required:
    - category: "Unverified completion"
    - claim_contains: "Ready to commit"
    - gap_mentions: ["dev server", "render", "console"]
    - verification_checks_include_behavioral: true

- case_id: trio_morphing_recall
  fixture: fixtures/trio_morphing_recall.jsonl
  mode: plan
  required:
    - category: "Unverified recall"
    - claim_contains: "morphing"
    - source_identified: true  # either named artifact or explicitly "unnamed"

- case_id: synthetic_plan_unverified_assumption
  fixture: fixtures/synthetic_plan_unverified_assumption.jsonl
  mode: plan
  required:
    - category: "Unverified assumption"
    - claim_contains: "lib/stripe.ts"
    - source_identified: true

- case_id: synthetic_completion_false_proxy
  fixture: fixtures/synthetic_completion_false_proxy.jsonl
  mode: completion
  required:
    - category: "Unverified completion"
    - claim_contains: "build passes"
    - gap_mentions: ["dev server", "click", "render"]
    - verification_checks_include_behavioral: true

# plan-critic cases. Reviewed by agents/plan-critic.md, not agents/auditor.md.
# The harness will need a `reviewer:` dispatch when live invocation lands;
# until then these are .expected.txt references like the rest.

- case_id: scope_drift_form_fix
  fixture: fixtures/scope_drift_form_fix.jsonl
  mode: plan
  reviewer: plan-critic
  required:
    - category: "Scope drift"
    - severity: "BLOCKER"
    - finding_count: 3
    - claim_contains: "Redesign the ContactForm"

- case_id: spec_violation_bundled_ui
  fixture: fixtures/spec_violation_bundled_ui.jsonl
  mode: plan
  reviewer: plan-critic
  required:
    - category: "Spec violation"
    - severity: "BLOCKER"
    - finding_count: 1
    - reference_rule_contains: "Never bundle UI changes with refactors"
    - claim_contains: "LoadingSkeleton"

- case_id: internal_incoherence_jwt_migration
  fixture: fixtures/internal_incoherence_jwt_migration.jsonl
  mode: plan
  reviewer: plan-critic
  required:
    - category: "Internal incoherence"
    - severity: "BLOCKER"
    - finding_count: 1
    - claim_contains: "session.ts"
