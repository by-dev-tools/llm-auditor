ISSUE · BLOCKER · Internal incoherence

Element A:
> "Keep src/middleware/session.ts active in the middleware chain to preserve existing user sessions during the transition"

Element B:
> "Remove src/middleware/session.ts and delete all session-cookie code paths, including the sessionStore dependency"

Conflict: step 3 keeps the session middleware active while step 4 removes it in the same plan; both cannot be true at the end of execution, and the plan does not resolve which wins.

Suggested: Choose one — either retain the session middleware behind a feature flag for a defined transition window (and defer its removal to a follow-up plan), or remove it outright as part of this migration. Revise the plan to reflect a single direction.
